# الأمان — المرحلة الخامسة

تمت إضافة أساسيات الأمان التالية:

1. كلمات المرور الجديدة لا تُخزن كنص صريح؛ يتم تخزين SHA-256.
2. الحساب القديم `admin/admin` يتم تحويله إلى hash عند أول تسجيل دخول ناجح في قواعد البيانات التي جاءت من المراحل السابقة.
3. الحسابات غير النشطة لا تستطيع تسجيل الدخول.
4. توجد أدوار وصلاحيات وصول للوظائف الرئيسية.
5. المستخدم لا يرى عناصر القائمة غير المسموح بها، ويتم أيضًا التحقق من الصلاحية عند فتح الصفحة.
6. يمكن للمدير إدارة المستخدمين وتفعيلهم أو إيقافهم.
7. يمكن للمستخدم تغيير كلمة مروره من داخل الإعدادات.

## قبل الاستخدام التجاري

- يوصى باستبدال SHA-256 البسيط بخوارزمية مخصصة لكلمات المرور مثل Argon2 أو bcrypt مع salt مناسب.
- يجب تفعيل قفل/تحديد محاولات تسجيل الدخول.
- يجب إضافة سجل تدقيق Audit Log للعمليات الحساسة.
- يجب حماية النسخ الاحتياطية وتشفيرها.
- يجب اختبار الصلاحيات على جهاز Android فعلي قبل النشر.


## المرحلة الثامنة
- حماية أساسية لتسجيل الدخول والنسخ الاحتياطي والاستعادة مع فحص SHA-256.
