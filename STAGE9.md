# المرحلة التاسعة — الاختبار والتدقيق وتجهيز الإنتاج

## ما تم تنفيذه
- إضافة شاشة **فحص سلامة النظام** داخل الإعدادات.
- فحص وجود جداول قاعدة البيانات المطلوبة.
- فحص وجود hash لحساب المدير.
- فحص أرصدة نقاط الولاء السالبة.
- فحص الدفعات التي تتجاوز إجمالي الفاتورة أو الفواتير ذات الإجمالي السالب.
- فحص الحجوزات ذات المدة غير الصحيحة أو العربون غير الصحيح.
- فحص تحميل صلاحيات الأدوار.
- الفحص يتم محليًا ولا يرسل بيانات التطبيق إلى الإنترنت.

## نتيجة التدقيق في بيئة البناء الحالية
- تم فحص بنية المشروع ووجود ملفات Flutter الأساسية يدويًا.
- تم فحص قاعدة البيانات وترقيات الإصدارات 1 → 8 في المصدر.
- تم فحص وجود مجلدات assets المطلوبة في المشروع.
- لم يتم تشغيل `flutter analyze` أو `flutter test` أو `flutter build apk` لأن Flutter/Dart SDK غير متوفر في بيئة التنفيذ الحالية.

## اختبار قبول مقترح قبل الإصدار
1. تسجيل الدخول بالحساب الإداري وتغيير كلمة المرور.
2. إنشاء خدمة وعميل ومعالج.
3. إنشاء حجز داخل ساعات العمل ثم تجربة حجز متعارض.
4. إنشاء فاتورة وتسجيل العربون وباقي الدفعة.
5. التأكد من عدم تجاوز الدفعات لإجمالي الفاتورة.
6. التحقق من نقاط الولاء وعدم احتساب الفاتورة مرتين.
7. تجربة أدوار الاستقبال والمعالج والمحاسب والتأكد من الصلاحيات.
8. إنشاء نسخة احتياطية والتحقق منها ثم تجربة الاستعادة على نسخة اختبارية.
9. اختبار WhatsApp والروابط والطقس مع وبدون إنترنت.
10. تشغيل `flutter analyze` و`flutter test` ثم بناء APK/AAB على جهاز/بيئة Flutter فعلية.
