# المرحلة الثامنة — الأمان والنسخ الاحتياطي والاستعادة

تمت إضافة طبقة أمان أساسية فوق المرحلة السابعة:

- ترقية قاعدة البيانات إلى الإصدار 8.
- منع مؤقت لتسجيل الدخول بعد 5 محاولات فاشلة لمدة 60 ثانية.
- تصفير عداد المحاولات عند نجاح الدخول.
- كلمات المرور محفوظة كـ SHA-256 hash، ولا يتم تخزين كلمة المرور الجديدة كنص صريح.
- نسخ احتياطي داخل مجلد `backups` في مساحة التطبيق.
- إنشاء ملف SHA-256 مرافق لكل نسخة للتحقق من سلامتها قبل الاستعادة.
- مشاركة النسخة الاحتياطية خارج التطبيق.
- استعراض النسخ الاحتياطية الداخلية والتحقق منها.
- استعادة نسخة احتياطية بعد تأكيد المستخدم.
- تنظيف ملفات SQLite المؤقتة WAL/SHM بعد الاستعادة.

## ملاحظة أمنية
هذه المرحلة تضيف حماية عملية للنسخ وسلامة الملفات، لكنها لا تعتبر تشفيرًا كاملًا لقاعدة البيانات أثناء التخزين. تشفير قاعدة البيانات نفسه يحتاج طبقة تشفير SQLite/Keystore مناسبة لنسخة الإنتاج.
